import Banner from "@/components/banner";
import NavBar from "@/components/nav";

const Header = () => {
  return (
    <header>
      <Banner />
      <NavBar />
    </header>
  );
};

export default Header;
